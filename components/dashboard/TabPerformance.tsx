'use client';
import Card from './Card';

export default function TabPerformance() {
  return (
    <Card title="Performance" style={{ minHeight: 180 }}>
      (Kommer snart) – Topopslag / tider / kanaler.
    </Card>
  );
}
