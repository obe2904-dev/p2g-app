'use client';
import Card from './Card';

export default function TabPlanning() {
  return (
    <Card title="Planlæg & udgiv" style={{ minHeight: 180 }}>
      (Kommer snart) – Kalender/planlægning.
    </Card>
  );
}
