'use client';

export default function DashboardPage() {
  return (
    <main style={{ padding: 16 }}>
      <h2>Dashboard test</h2>
      <p>Hvis du kan se denne tekst i browseren på <code>/dashboard</code>, så virker selve ruten.</p>
    </main>
  );
}
