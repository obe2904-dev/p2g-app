'use client';
import { useState } from 'react';

export default function ClientSmoke() {
  const [n, setN] = useState(0);
  return (
    <main style={{ padding: 16 }}>
      <p>Client component kører ✔</p>
      <button onClick={() => setN(n + 1)}>Klik: {n}</button>
    </main>
  );
}
